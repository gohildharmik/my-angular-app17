name: Angular CI/CD to IIS

on:
  push:
    branches: [main]   # Change if your main branch is named differently

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Install Dependencies
      run: npm install

    - name: Build Angular App
      run: npm run build -- --configuration=production

    # - name: Deploy to IIS Server via SCP
    #   uses: gohildharmik/my-angular-app17@v1.0.4
    #   with:
    #     local: dist/my-anglular-app/
    #     remote: /c$/inetpub/     
    #     host: ${{ '2405:204:8104:d116:c4a7:1179:3582:7f3b' }}
    #     username: ${{ 'admin' }}
    #     password: ${{ 'swami123' }}
      
    - name: Deploy via scp
      run: scp -r dist/my-anglular-app/ admin@2405:204:8104:d116:c4a7:1179:3582:7f3b:/c/inetpub/
      env:
        SSHPASS: ${{ 'swami123' }}